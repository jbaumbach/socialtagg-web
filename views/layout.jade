//-
//- The layout of the app.  The layout describes how the a group of pages will look.  This is similar
//- to the "Master Page" pattern in .NET.  Your individual .jade pages will define the content
//- that plugs into the "block" sections in this file.
//-
doctype 5
html
  head
    title= title
      |  - SocialTagg
    link(rel='stylesheet', href='/stylesheets/style.css')
    link(rel='shortcut icon', href='/images/favicon.ico')
    
    //- Freeze the view in mobile devices.  They won't be able to zoom in.
    meta(name="viewport", content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no")
    
    //- Import the website analytics 
    //-
    //- Side note: cool way to specify scripts to load from the controller:
    //- http://stackoverflow.com/questions/5605392/node-js-with-express-importing-client-side-javascript-using-script-tags-in-jade
    //-
    include includes/analytics
  body
    header
      h1 
        a(href="/") Social
          span Tagg
    .container
      .main-content
        //-
        //- If we have a message passed to us, let's display it.  This is an example of scripting
        //- in Jade.
        //-
        - if (locals.message) 
          p.message #{ locals.message }
        //-
        //- If we have an error message, let's display it.
        //-
        - if (locals.error)
          p.error #{ locals.error }
        
        //-
        //- Your subclass should define "content" and optionally "sidebar"
        //-
        block content
      .sidebar
        block sidebar
    footer
      p Copyright (C) 2013 SocialTagg, LLC