mixin eventanalyticssummary(image, id, description)

  .five.columns
    img.at-glance(src='/images/eventanalytics/' + image)
    
    p.at-glance.value(ng-show='!dataResults["#{ id }"]') ...
    
    p.at-glance.value(ng-show='dataResults["#{ id }"]', style="display:none;") 
      | {{ dataResults["#{ id }"] }}
    
    p.at-glance.desc #{ description }
